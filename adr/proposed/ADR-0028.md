---
id: "ADR-0028"
title: "CEREBRO Embeddings: Vertex AI Transitorio com Plano de Migração Local-First"
status: proposed
date: "2026-02-14"

authors:
  - name: "AI Agent"
    role: "Development Assistant"
    github: "securellm-mcp"
  - name: "Pina"
    role: "Security Engineer & Architect"
    github: "pina"

reviewers: []

governance:
  classification: "critical"
  requires_approval_from:
    - architect
  compliance_tags:
    - "cost"
    - "vendor-lock-in"
    - "infrastructure"
    - "AI"
  review_deadline: "2026-02-28"
  auto_supersede_after: "6m"

scope:
  projects:
    - CEREBRO
    - SECURELLM-MCP
  layers:
    - infrastructure
    - ml
  environments:
    - all

rationale:
  drivers:
    - "$6k GCP credits disponiveis — decisao oportunista de aproveitar enquanto existem"
    - "Vertex AI text-embedding-004 oferece qualidade SOTA (768-dim) sem custo imediato"
    - "Creditos expiram em ~3 meses (~maio 2026) — necessidade de plano de saida"
    - "Servico imperative systemd crashou 1630+ vezes por path stale, depois por vertexai ausente no flake"
    - "securellm-mcp semantic cache precisa de embeddings para deduplicacao de tool calls"
    - "ADR-0007 estabelece Local-First como estrategia — Vertex AI e excepcao temporaria"
  alternatives_considered:
    - name: "Manter Vertex AI permanentemente"
      rejected_reason: "Viola ADR-0007, cria vendor lock-in, custo recorrente apos creditos"
    - name: "Remover cerebro-embeddings imediatamente"
      rejected_reason: "Desperdiça $6k em creditos ja disponiveis, qualidade superior gratuita"
    - name: "Usar apenas fallback char-frequency do securellm-mcp"
      rejected_reason: "Qualidade muito baixa, nao captura semantica real"
  trade_offs:
    - "Vertex AI temporario aproveita creditos, mas cria dependencia que precisa ser gerenciada"
    - "Manter dois caminhos (Vertex + fallback local) adiciona complexidade mas evita lock-in"
    - "768-dim Vertex vs 384-dim local requer tratamento de dimensoes no cache"

consequences:
  positive:
    - "Aproveita $6k de creditos GCP que seriam desperdicados"
    - "Qualidade superior de embeddings durante periodo transitorio"
    - "Plano de migracao documentado evita surpresas quando creditos acabarem"
    - "API compativel (/embedding endpoint) facilita swap de provider"
  negative:
    - "Servico precisa ser corrigido (vertexai no flake.nix, migrar para home-manager)"
    - "Dimensoes diferentes (768 vs 384) requerem atencao na migracao"
    - "Dependencia temporaria de internet e GCP"
  risks:
    - risk: "Creditos GCP acabam sem migracao pronta"
      probability: "medium"
      impact: "high"
      mitigation: "Deadline firme: migracao para llama.cpp implementada ate 2026-04-15 (1 mes antes)"
    - risk: "Servico volta a crash-loopear consumindo recursos"
      probability: "medium"
      impact: "medium"
      mitigation: "Migrar para home-manager declarativo (nix garante deps e paths corretos)"
    - risk: "Dados de cache ficam incompativeis na migracao (768→384 dim)"
      probability: "high"
      impact: "low"
      mitigation: "Cache SQLite e efemero (TTL 1h default), limpar na migracao e suficiente"
    - risk: "Vertex AI muda API ou pricing"
      probability: "low"
      impact: "medium"
      mitigation: "API wrapper abstrai o provider; migracao independe de mudanca do Vertex"

sunset:
  date: "2026-05-15"
  reason: "GCP credits expiram ~maio 2026; substituido por ADR-0035 (Hybrid Reranker local-first)"
  successor: "ADR-0035"

implementation:
  effort: "small"
  timeline: "Manter como esta ate sunset (2026-05-15), sem investimento adicional"
  dependencies:
    - "cerebro flake.nix com vertexai no devShell (se for corrigido)"
    - "home-manager systemd.user.services (se migrar de imperative)"
  blocked_by: []
  tasks:
    - "Manter servico como esta (disabled) — nao investir esforco em fix"
    - "ADR-0035 implementa substituto local-first antes do sunset"
    - "2026-05-15: remover cerebro-embeddings service definitivamente"
    - "2026-05-15: atualizar este ADR para status 'superseded' por ADR-0035"

relations:
  supersedes: []
  superseded_by: "ADR-0035"
  related_to:
    - "ADR-0007"
    - "ADR-0034"
    - "ADR-0035"
  implements: []
  enables:
    - "ADR-0035 — substituto hybrid reranker local-first"

knowledge_extraction:
  keywords:
    - "vertex-ai"
    - "embeddings"
    - "GCP credits"
    - "vendor lock-in"
    - "migration plan"
    - "semantic cache"
    - "llama.cpp"
    - "local-first"
    - "cerebro"
    - "securellm-mcp"
  concepts:
    - "Decisao oportunista temporaria vs estrategia de longo prazo"
    - "Anti-lock-in: API compativel permite swap de provider sem mudanca de consumidor"
    - "Creditos cloud como recurso finito — exigem exit strategy documentada"
    - "Embedding dimension mismatch requer invalidacao de cache na migracao"
  questions_answered:
    - "Por que cerebro-embeddings usa Vertex AI em vez de local?"
    - "Quando os creditos GCP expiram e qual o impacto?"
    - "Qual o plano de migracao para local-first quando creditos acabarem?"
    - "Como evitar vendor lock-in com Vertex AI?"
    - "Por que o servico crashava em loop?"
  embedding_priority: "high"

audit:
  created_at: "2026-02-14T10:49:18.968Z"
  last_modified: "2026-02-15T01:00:00.000Z"
  version: 4
  changelog:
    - date: "2026-02-14T10:49:18.968Z"
      author: "securellm-mcp"
      change: "ADR criado — foco em migracao de systemd imperative para home-manager"
      commit_hash: null
    - date: "2026-02-14T23:10:00.000Z"
      author: "claude-opus-4.6"
      change: "Adicionado diagnostico completo do servico (disabled, port 8001 down)"
      commit_hash: null
    - date: "2026-02-15T00:30:00.000Z"
      author: "claude-opus-4.6"
      change: "Reescrito: decisao oportunista documentada, plano de migracao 3 fases, calculo de risco de creditos, anti-lock-in"
      commit_hash: null
    - date: "2026-02-15T01:00:00.000Z"
      author: "claude-opus-4.6"
      change: "Mudanca de direcao: sunset mode, sem fix, substituido por ADR-0035 (Hybrid Reranker local-first)"
      commit_hash: null
---

## Contexto

### Por que Vertex AI existe no CEREBRO?

Em 11 de janeiro de 2026, **ADR-0007** estabeleceu a estrategia **Local-First AI** para PHANTOM e CEREBRO, com embeddings via `sentence-transformers/all-MiniLM-L6-v2` (384-dim, custo $0).

**4 dias depois** (15 jan, commit `63e71f9`), o `embedding_server.py` foi adicionado ao CEREBRO usando **Vertex AI text-embedding-004** (768-dim, cloud). A motivacao:

> **$6k de creditos GCP disponiveis** — decisao oportunista de aproveitar qualidade SOTA sem custo imediato.

Nenhum ADR foi criado na epoca documentando essa excecao ao ADR-0007. O comentario no `start_embedding_server.sh` linha 21 confirma a intencao:
```
💰 Ready to burn those $6k credits! 🔥
```

### O que deu errado

1. **Crash loop #1** (1630+ restarts): `WorkingDirectory` apontava para `/home/kernelcore/arch/cerebro` — path stale apos mover repo para `/home/kernelcore/master/cerebro`
2. **Crash loop #2** (18,192+ linhas de log): `ModuleNotFoundError: No module named 'vertexai'` — o `flake.nix` nao inclui vertexai, e o shellHook do Poetry nao executa no contexto do systemd ExecStart
3. **Nunca conectado ao consumidor**: securellm-mcp aponta para `localhost:8081` (llama.cpp), nao `8001` (cerebro)
4. **Dimensoes incompativeis**: Vertex AI retorna 768-dim, fallback do securellm-mcp usa 384-dim

### Consumidor: securellm-mcp Semantic Cache

O `SemanticCache` em `securellm-mcp/src/middleware/semantic-cache.ts` usa embeddings para deduplicar tool calls MCP. Fluxo:

```
Tool call recebida → generateEmbedding(query_text)
    → fetch(LLAMA_CPP_URL/embedding, {content: text})
    → cosineSimilarity(query, cached_entries)
    → Se similarity > 0.85 → cache hit (evita chamada ao LLM)
```

Config atual (`src/types/semantic-cache.ts:88`):
```typescript
llamaCppUrl: process.env.LLAMA_CPP_URL || 'http://localhost:8081'
```

Fallback quando embedding server indisponivel (`semantic-cache.ts:200-228`):
```typescript
// Char frequency hash → Float32Array(384) — rapido mas impreciso
private fallbackEmbedding(text: string): Float32Array { ... }
```

### API Compativel (Anti-Lock-In Natural)

O cerebro-embeddings ja foi desenhado como **drop-in replacement** do llama.cpp:

| Aspecto | llama.cpp (8081) | cerebro-embeddings (8001) |
|---------|------------------|---------------------------|
| Endpoint | `POST /embedding` | `POST /embedding` |
| Request | `{ content: string }` | `{ content: string }` |
| Response | `{ embedding: float[] }` | `{ embedding: float[], dimension, model, cost }` |
| Dimensao | Depende do modelo | 768 (text-embedding-004) |

A troca de provider requer **apenas mudar a URL/porta**. Zero mudanca de codigo.

## Decisao

**Cerebro-embeddings Vertex AI entra em modo sunset.** Nao sera corrigido nem receberah investimento adicional. Permanece como esta (disabled) ate a data de sunset (2026-05-15), quando sera removido definitivamente.

O substituto e o **ADR-0035** — um novo projeto focado em reranking hibrido inteligente com arquitetura local-first e GCP como fallback opcional (aproveitando os creditos restantes).

### Sunset Timeline

```
FEV 2026 ─── Agora ────────────────────────────────────────────┐
              - Cerebro-embeddings: congelado (disabled, sem fix) │
              - ADR-0035: projeto substituto iniciado              │
              - Servico imperative permanece no disco (inativo)    │
                                                                   │
MAI 2026 ─── Sunset ──────────────────────────────────────────────┘
              - Remover ~/.config/systemd/user/cerebro-embeddings.service
              - Creditos GCP expiram (sem impacto — ja nao e primario)
              - Este ADR → status 'superseded' por ADR-0035
              - Vertex AI vira fallback opcional no novo projeto
```

### Por que nao corrigir o cerebro-embeddings

1. **Investir em fix de um servico com 3 meses de vida restante nao compensa**
2. O problema real nao e infra (systemd, flake.nix) — e arquitetural (cloud-first viola ADR-0007)
3. O substituto (ADR-0035) resolve o problema de forma definitiva com local-first
4. GCP Vertex AI sobrevive como **fallback** dentro do novo projeto, nao como servico standalone

## Implementacao

### Acao Unica: Congelar e Agendar Remocao

Nenhuma acao de implementacao neste ADR. O servico permanece disabled ate o sunset.

Na data de sunset (2026-05-15):
```bash
rm ~/.config/systemd/user/cerebro-embeddings.service
systemctl --user daemon-reload
```

Todo o esforco de implementacao vai para o **ADR-0035**.

## Diagnostico Historico do Servico

### Crash Loop #1: Path Stale

```
WorkingDirectory=/home/kernelcore/arch/cerebro  ← path antigo
Realidade: /home/kernelcore/master/cerebro      ← path atual
Resultado: 1630+ restarts, 5s cada = ~2.3 horas de CPU desperdicada
```

### Crash Loop #2: Modulo Ausente

```
$ nix develop --command python -c "import vertexai"
ModuleNotFoundError: No module named 'vertexai'

Causa: flake.nix nao inclui google-cloud-aiplatform
       shellHook do Poetry (poetry install) nao executa no systemd ExecStart
Resultado: 18,192+ linhas de log, ~387MB RAM por tentativa
```

### Estado Atual (2026-02-14)

```
$ systemctl --user is-enabled cerebro-embeddings
disabled

$ systemctl --user is-active cerebro-embeddings
inactive

$ ss -tlnp | grep 8001
(nada — porta nao esta escutando)
```

## Referencias

- ADR-0007: Local-First AI Strategy (aceito, 2026-01-11) — estrategia que este ADR excepciona temporariamente
- ADR-0034: Phantom Test Coverage Gaps — documenta dependencias de embedding nos testes
- Commit `63e71f9` (2026-01-15): feat(embedding-server) — adicao do Vertex AI sem ADR
- cerebro flake.nix: `/home/kernelcore/master/cerebro/flake.nix`
- Embedding server: `/home/kernelcore/master/cerebro/src/phantom/core/embedding_server.py`
- securellm-mcp semantic cache: `/home/kernelcore/master/securellm-mcp/src/middleware/semantic-cache.ts`
- securellm-mcp config: `/home/kernelcore/master/securellm-mcp/src/types/semantic-cache.ts`
- Servico imperative: `~/.config/systemd/user/cerebro-embeddings.service`
- home-manager pattern: `/etc/nixos/hosts/kernelcore/home/`
